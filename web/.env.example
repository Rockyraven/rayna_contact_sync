# Optional: only set this to override the automatic local/production choice
# in src/config.ts (e.g. to point at an ngrok tunnel instead).
# VITE_API_BASE_URL=http://localhost:4001
VITE_GOOGLE_CLIENT_ID=425674498204-m82k36csatg4pu7t5ctr25esteopjo95.apps.googleusercontent.com
