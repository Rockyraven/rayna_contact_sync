DATABASE_URL=postgres://rayna_app:rayna_dev_pw@localhost:5432/rayna_contact_sync
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
JWT_SECRET=
# Base64-encoded 32-byte key used to encrypt linked Gmail refresh tokens at rest.
# Generate with: openssl rand -base64 32
ENCRYPTION_KEY=
# Comma-separated emails allowed to use the admin web app's cross-user views.
ADMIN_EMAILS=vaibhav@raynatours.com,rocky.86agency@gmail.com
# Comma-separated origins allowed to call this API from a browser (the admin web app).
CORS_ORIGIN=http://localhost:5173
PORT=4000
